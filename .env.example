# Path to local Open Mercato monorepo (optional).
# If not set, skills will auto-clone to .cache/open-mercato/ on first use.
# OM team: point this to your local checkout for faster access.
# OM_REPO=~/path/to/open-mercato
